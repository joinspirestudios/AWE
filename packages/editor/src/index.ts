/**
 * @app/editor
 *
 * Konva-based design editor. Empty for now — implemented after the AI
 * funnel is wired. Will export:
 *   - <Editor> component (the canvas + chrome)
 *   - useEditorStore() (Zustand store for editor state)
 *   - scene-engine primitives (snapping, hit-testing, transforms)
 *   - exporters (PNG/JPG/WebP/PDF/SVG)
 */

export const __placeholder = true
